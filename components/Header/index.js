import React from "react";
import Button from "../Button";
const Header = ({ handleWorkScroll, handleAboutScroll }) => {
  return (
    <div className="mt-6 flex items-center justify-between sticky top-0 z-10 bg-white">
        {/* eslint-disable-next-line react/no-unescaped-entities */}
      <h1 className="font-medium mob:p-2 laptop:p-0">Scott and Jess's slightly weird attempt at a thank you... (Entirely Scott's idea)</h1>
      <div>

      </div>
    </div>
  );
};

export default Header;
